// 향후 이벤트 핸들러를 추가할 때 사용할 예시 파일입니다.
// 예시: ready.js

/*
const { Events } = require('discord.js');

module.exports = {
    name: Events.ClientReady,
    once: true,
    execute(client) {
        console.log(`Ready! Logged in as ${client.user.tag}`);
    },
};
*/
