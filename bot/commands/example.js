// 향후 slash command를 추가할 때 사용할 예시 파일입니다.
// 예시: ping.js

/*
const { SlashCommandBuilder } = require('discord.js');

module.exports = {
    data: new SlashCommandBuilder()
        .setName('ping')
        .setDescription('서버의 응답 시간을 확인합니다.'),
    async execute(interaction) {
        await interaction.reply('Pong!');
    },
};
*/
